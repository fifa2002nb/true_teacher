specVersion: "v1"
serviceId: "trueteacher"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "trueteacher-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
